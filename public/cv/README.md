# CV folder

This folder holds the CV PDF served by the portfolio:

```
Ali-Raza-Mahmood-CV.pdf
```

Public path: `/cv/Ali-Raza-Mahmood-CV.pdf`

`CV_URL` in `src/components/CVDownload.jsx` already points to this path, so the
Download CV button is active. To update the CV, replace this PDF file with the
same filename.